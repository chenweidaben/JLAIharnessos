# 安全策略（Security Policy）

健澜科技杠OS 处理高度敏感的医疗数据，安全是项目的第一优先级。本文件说明如何报告漏洞、我们的响应流程，以及平台内置的安全机制。

## 支持版本

| 版本 | 安全更新 |
| ---- | -------- |
| 0.1.x（当前主干） | ✅ 积极维护 |
| 更早的内部版本 | ❌ 不提供公开更新 |

## 报告安全漏洞

**请不要通过公开 GitHub Issue、讨论区或 Pull Request 披露安全漏洞。**

请通过以下方式私下报告：

- 邮件：`security@jianlan.tech`（占位地址，正式发布前以官网公布为准）
- 邮件标题以 `[Security] Gang-OS: ` 开头
- 建议包含：影响版本、复现步骤（PoC）、预期与实际结果、影响评估、您的联系方式

### 响应 SLA

| 阶段 | 目标时间 |
| ---- | -------- |
| 确认收到 | 48 小时内 |
| 初步评估与分级 | 5 个工作日内 |
| 高危/严重漏洞缓解方案或补丁 | 2 周内 |
| 修复发布与公告（协调披露） | 与报告者协商后 |

我们欢迎负责任披露，并会在公告中致谢（除非您希望匿名）。请在补丁发布前给予我们合理的修复窗口，**不要在未授权情况下对非本人系统进行测试**。

## 威胁模型与内置防护

- **身份与授权**：JWT（HMAC-SHA256 验签，拒绝篡改/伪造）、刷新令牌轮换、RBAC + ABAC、最小权限、管理接口角色守卫、WebSocket 升级鉴权。
- **输入安全**：编排层表达式使用**自研安全沙箱**（词法/递归下降解析 + 白名单函数，无 `eval`/`new Function`，拦截 `process/global/require/constructor/__proto__` 等危险根）；Prompt 注入五层防护；SQL 注入、XSS、命令注入过滤。
- **数据安全**：四级数据分级（L1 公开–L4 机密）、16 类敏感数据脱敏、AES-256-GCM 加密、传输 TLS、字段级隐私保护。
- **审计与追溯**：关键操作审计日志采用**哈希链**防篡改，支持完整性校验；智能体每次工具调用、人工确认、风险拦截均可追溯。
- **医疗安全**：工具按 low/medium/high 风险分级；高风险动作强制人工确认，处方等支持双人复核；CDS 主动提醒与拦截；输出仅为辅助建议并附免责声明。
- **身份认证**：JWT（HMAC-SHA256，校验 exp/nbf/iss）+ RBAC/ABAC；内置 **TOTP 多因素认证**（RFC 6238、±1 步时钟容忍、同口令重放拒绝、一次性备份码），高风险角色可强制二次校验。
- **基础设施**：PostgreSQL SCRAM-SHA-256、WAL 归档 PITR；Redis 禁用/重命名 `KEYS/FLUSHDB/FLUSHALL/DEBUG`、密码不入库；安全响应头（CSP、X-Frame-Options、CSRF）；依赖漏洞扫描。
- **合规设计**：参照网络安全等级保护三级、《网络安全法》《数据安全法》《个人信息保护法》《医疗卫生机构网络安全管理办法》。

## 部署方安全责任（共享责任模型）

开源代码提供安全能力，但生产安全需要部署方共同承担：

1. 修改所有默认口令，通过环境变量/密钥管理注入强随机 `JWT_SECRET` 与 `ENCRYPTION_MASTER_KEY`；
2. 启用 HTTPS/TLS，并为管理员与临床高权限角色强制启用内置 **TOTP 多因素认证（MFA）**
   （RFC 6238，兼容主流验证器，含一次性备份码与重放保护；接口见 `/api/v1/auth/mfa/*`，
   生产多副本须将 MFA 存储替换为 PostgreSQL/Redis 共享实现），再按等保三级完成主机、网络与制度建设；
3. 数据不出院/不出境，落实访问控制、最小授权与定期审计；
4. 及时升级版本、跟踪依赖安全公告；
5. 接入真实 HIS 前在隔离环境完成安全测试与临床验证。

## 已知限制

- 当前为开源 0.1.x：**TOTP MFA 全链路已内置**——后端绑定/校验/备份码/重放保护，
  前端「个人中心 → 安全设置」提供扫码绑定（otpauth 二维码）、6 位动态码确认、
  一次性备份码离线保存与校验后停用（组件 `web/src/pages/profile/MfaSettings.tsx`），
  并提供 PostgreSQL 共享存储 `PgMfaStore`（表 `iam.mfa_factors`，密钥 AES-GCM 加密落库，已验证多副本状态一致）；
  BFF 默认仍为进程内存储，**多副本生产部署需在装配处切换为 `PgMfaStore` 并接入连接池**，
  登录流程的二次验证接入与国密算法（SM2/SM3/SM4）、完整 KMS/硬件加密机集成仍在路线图中；
- BFF `/metrics` 默认依赖内网网络策略隔离，如跨网络暴露请配置 `METRICS_TOKEN` 或反向代理鉴权；
- 内置适配器多为接口与 Mock 实现，真实厂商联调需补充凭证与证书；
- 示例患者数据全部为**虚构数据**，禁止导入真实未脱敏患者信息到演示环境。

感谢帮助守护患者数据安全的每一位研究者与贡献者。
