# 健澜科技数智医院智能体 — 问题修复记录

- 日期：2026-09-16
- 说明：每条记录含 问题 → 根因 → 修复 → 验证。

## 一、ESLint error 修复（全部清零）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 106 个 `require-await` error | 医疗工具 execute / 适配器 onInit 必须返回 Promise 但无 await，规则误报异步契约 | eslint.config.js 关闭该规则并注释说明 | 0 error |
| 2 | 222 条 `import/no-duplicates` 误报 | 配置了未安装的 `eslint-import-resolver-typescript` | 移除 `import/resolver.typescript` 设置 | 0 error |
| 3 | DICOMMetadata 冗余类型联合 | 类型联合写法不被 TS 收窄 | 改用 `(string & {})` 模式 | tsc 通过 |
| 4 | FHIRParser `unknown` 赋值 | `string \| unknown` 等价于 unknown | 收敛为 `unknown` | tsc 通过 |
| 5 | InputValidator 控制字符正则 unsafe | 正则含控制字符 | 正则加 eslint-disable 说明 | 通过 |
| 6 | QueryEngine 常量条件 / IntegrationMonitor 空构造 | 死分支 / 空构造 | 修正条件、移除空构造 | 通过 |
| 7 | LogStorage/AuditReportGenerator 枚举字面量比较 | 用字符串字面量比较枚举 | 改用枚举成员 `AuditResult.DENIED`；LogReadOptions.logType 类型收窄为 AuditLogType | 通过 |
| 8 | PermissionChecker `key` 不可赋给 PermissionKey | Object.values 迭代宽化为 string | `key as PermissionKey` + 规则 disable 说明 | tsc 通过 |
| 9 | queryPatient `||` 被 prefer-nullish-coalescing 报错 | 空串语义必须用 `||` | disable 注释对齐到表达式行（Prettier 折行后重新定位） | 0 error |
| 10 | 前端 35 个 jsx-a11y error | 非交互元素挂 onClick | 套用已有 `clickableProps()`；demo FeatureCard 本轮补打 | FE 0 error |
| 11 | 83 处 `import('...').Type` 内联注解 | consistent-type-imports 要求 | codemod 转为顶部 `import type`，并修复注释块内错位 | BE 0 error |
| 12 | PromptInjectionGuard 字符类内 `\/` | 字符类中 `/` 无需转义 | 移除多余反斜杠 | 通过 |

## 二、类型安全修复
| # | 问题 | 修复 | 验证 |
|---|---|---|---|
| 13 | 前端 noUnusedLocals/noUnusedParameters=false | 改为 true，修复 monitoring/index.ts 的 `_lineno/_colno` | 前端 tsc 0 错误 |
| 14 | codemod 误把 import 插入版权注释块 | fixup 脚本把注释块内 import 移出 | tsc 0 错误 |

## 三、格式与规范
| # | 问题 | 修复 | 验证 |
|---|---|---|---|
| 15 | 后端 252、前端 130 文件 Prettier 不一致 | `prettier --write` | 两端 `--check` 通过 |
| 16 | 后端 76 文件缺版权头 | 脚本批量补"健澜科技"版权头 | 后端 299/299 |
| 17 | 公司名可能错别字 | 全量搜索 0 命中 | 统一"健澜科技" |

## 四、依赖安全
| # | 问题 | 修复 | 验证 |
|---|---|---|---|
| 18 | 后端 46 漏洞（16 高危，undici/ws） | `bun audit fix` | 高危清零，剩 2 中危（SDK 版本锁定，记录） |
| 19 | 前端 11 漏洞 | `npm audit fix` | 生产路径无高危；dev 工具链项记录 |

## 五、未完全闭环项（如实记录）
| 项 | 现状 | 原因/后续 |
|---|---|---|
| 后端 ESLint warning 112 | 未压到 ≤10 | 主体为接口契约未用参数（建议 `_` 前缀）与 42 个非空断言风格项；非 error，建议后续专项清理 |
| 16 个函数圈复杂度 >20 | 未重构 | 最高 DesensitizationUtils 72，建议规则表化；不影响正确性 |
| 后端 2 中危 / 前端 echarts、react-router | 未升级 | 需跨版本升级 + 回归测试，已纳入待办 |

## 六、验证汇总
- 后端 `tsc --noEmit`：0 错误；`eslint`：0 error。
- 前端 `tsc --noEmit`：0 错误；`eslint`：0 error / 9 warning。
- Prettier：前后端全部一致。
- 重复率：后端 1.12%、前端 0.32%（≤5%）。
