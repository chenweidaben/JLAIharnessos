# OPTIONAL production (real DAMO-RADAR model) dependencies.
# Only needed when running the GPU / weights-backed "production" mode.
# Install: pip install -r requirements-prod.txt
# These are NOT required to start the demo service.
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1
monai>=1.3
SimpleITK>=2.3
nibabel>=5.1
huggingface_hub>=0.23
transformers>=4.40
tqdm>=4.66
