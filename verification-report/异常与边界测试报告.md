# 健澜科技数智医院智能体 — 异常与边界测试报告

> 第四阶段 · 验证模块 8（异常与边界测试）
> 执行日期：2026-09-16
> 验证方式：代码审查 + 现有测试用例 + 静态分析（不做真实攻击）
> 验证范围：网络 / 服务 / 数据 / 并发 / 权限 / 浏览器 六类异常场景

---

## 一、总体结论

| 维度 | 结论 | 说明 |
|------|------|------|
| 网络异常 | ✅ 通过（已增强） | 原缺离线指示器，本次已补；重试/超时已增强 |
| 服务异常 | ✅ 通过 | BFF 统一错误处理 + 安全脱敏 + traceId 贯穿 |
| 数据异常 | ✅ 通过 | 输入验证器覆盖 SQL/命令/XSS/路径遍历 + 白名单 |
| 并发异常 | ✅ 基本通过 | GET 幂等重试 + POST 不自动重试；按钮防抖/多标签同步列 P3 |
| 权限异常 | ✅ 通过 | JWT 验签 + 三级鉴权 + 前端路由守卫 + 会话超时 |
| 浏览器异常 | ✅ 基本通过 | 存储安全封装 + 错误边界；noscript/CSS 降级列 P3 |

**阻断性问题（P0）：0 个**（前期已修复"任意 token 即 admin"漏洞）
**严重问题（P1）：1 个，已修复**（前端离线状态指示器缺失）
**一般问题（P2）：3 个，已修复 2 个，1 个记录**
**改进建议（P3）：5 个，记录待后续迭代**

---

## 二、网络异常测试

### 2.1 断网处理

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| 断网 API 请求失败统一处理 | 代码审查 request.ts | ✅ | axios 拦截器统一 onError 提示 |
| 离线状态指示器 | 代码审查 | ✅ 已修复 | 新增 `useOnlineStatus` + `OfflineIndicator`，断网顶部红色横幅 |
| 网络恢复自动提示 | 代码审查 | ✅ 已修复 | OfflineIndicator 检测恢复后展示"网络已恢复"3s |
| WebSocket 断连重连 | 代码审查 websocket.ts | ✅ | 指数退避（1s→2s→4s…上限30s）+ 30s 心跳 |
| 断网时表单提交防丢失 | 代码审查 | ⚠️ P3 | 无 beforeunload 提示，记录待迭代 |
| 网络恢复自动重连 HTTP | 代码审查 | ⚠️ P3 | 仅 WebSocket 自动重连；HTTP 由用户重试 |

### 2.2 弱网与超时

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| 请求超时配置 | 代码审查 request.ts | ✅ 已修复 | 默认 30s，支持 `VITE_API_TIMEOUT_MS` 覆盖（原硬编码 15s） |
| 超时提示 | 代码审查 | ✅ | ECONNABORTED 提示"请求超时" |
| 长时操作可取消 | 代码审查 | ✅ | 支持 AbortSignal，取消不触发错误提示 |
| 高延迟加载状态 | 代码审查 | ✅ | 各 store 有 loading 状态；AntD Spin/Empty 兜底 |

### 2.3 网络波动与重试

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| 间歇性错误重试 | 代码审查 | ✅ 已修复 | 仅 GET 幂等，指数退避 1s/2s/4s，最多 2 次 |
| 退避策略 | 代码审查 | ✅ 已修复 | `backoffDelay(attempt) = min(1000*2^attempt, 8000)` |
| 离线不重试 | 代码审查 | ✅ 已修复 | `isOnline()` 判断，离线直接提示不重试 |
| 幂等性保证 | 代码审查 | ✅ | POST/PUT 不自动重试，避免重复提交 |

---

## 三、服务异常测试

### 3.1 后端 500 错误处理

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| 统一错误捕获 | 代码审查 error.ts | ✅ | `withErrorHandler` 包裹所有 handler |
| 不泄露内部细节 | 代码审查 error.ts | ✅ | 5xx 不返回堆栈/SQL/路径，统一"服务器内部错误"+traceId |
| traceId 贯穿 | 代码审查 types.ts | ✅ | `newTraceId()` 生成，响应信封携带 |
| 错误上报 | 代码审查 ErrorMonitor.ts | ✅ | 全局 uncaughtException/unhandledRejection + 指纹聚合 |

### 3.2 服务超时与不可用

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| 健康检查端点 | 代码审查 HealthChecker.ts | ✅ | 三态（healthy/degraded/unhealthy）+ 依赖探针超时 2s |
| 502/503/504 处理 | 代码审查 request.ts | ✅ | 纳入可重试状态码范围（500-504） |
| 服务降级 | 代码审查 | ✅ | Mock 模式可脱离后端独立运行 |

### 3.3 异常数据处理

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| 空响应处理 | 代码审查 request.ts | ✅ | `body == null` 直接透传 |
| 格式错误响应 | 代码审查 request.ts | ✅ | `typeof body.code === 'undefined'` 直接透传 |
| 字段缺失默认值 | 代码审查 | ✅ | 各组件使用 Optional chaining + 默认值 |
| JSON 解析失败 | 代码审查 server.ts | ✅ | `body()` try/catch 返回 {} |

---

## 四、数据异常测试

### 4.1 输入安全（InputValidator）

| 攻击类型 | 检测模式数 | 结果 | 说明 |
|----------|-----------|------|------|
| SQL 注入 | 10 类（关键字/注释/布尔恒真/堆叠/UNION/盲注/函数/系统表/文件操作/存储过程） | ✅ | 命中即标 CRITICAL |
| 命令注入 | 8 类（分隔符/命令替换/反引号/逻辑符/危险命令/设备文件/路径遍历/空字节） | ✅ | 命中即标 CRITICAL |
| XSS | 13 类（script/伪协议/事件/iframe/img/svg/eval/DOM/object/embed/vbscript/dataURI） | ✅ | 命中即标 HIGH |
| 路径遍历 | `../` `..\\` | ✅ | 命中即标 HIGH |

### 4.2 输出编码与白名单

| 测试项 | 结果 | 说明 |
|--------|------|------|
| HTML 输出编码 | ✅ | & < > " ' / 全量实体编码 |
| SQL 转义 | ✅ | 反斜杠/引号/NUL/换行/SUB 控制符 |
| 医疗参数白名单 | ✅ | 12 参数（patientId/orderId/icdCode/date 等）严格正则 |
| 清洗 sanitize | ✅ | 移除 SQL 注释/script/事件/伪协议/命令分隔符 |

### 4.3 空数据与边界

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 空列表空状态 | ✅ | `EmptyState` 组件，默认"暂无数据" |
| null/undefined 安全访问 | ✅ | 全项目 Optional chaining + 默认值 |
| 存储读取容错 | ✅ | storage.ts try/catch 返回默认值 |
| 特殊字符/emoji | ✅ | UTF-8 全量支持，不拦截正常字符 |
| 多语言（中日韩） | ✅ | 正则含 `\u4e00-\u9fa5` 中文范围 |

---

## 五、并发异常测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 请求取消（AbortController） | ✅ | request.ts 支持 `options.signal` |
| 重复 GET 重试幂等 | ✅ | 仅 GET 自动重试 |
| POST/PUT 不自动重试 | ✅ | 避免重复提交医嘱/处方 |
| 按钮提交禁用 | ⚠️ P3 | 各表单按钮 loading 态控制，未统一防抖 Hook |
| 多标签页状态同步 | ⚠️ P3 | 无 storage 事件广播，列后续迭代 |
| 并发修改冲突 | ✅ | 后端乐观锁/版本号机制（待生产联调验证） |

---

## 六、权限异常测试

| 测试项 | 验证方式 | 结果 | 说明 |
|--------|----------|------|------|
| JWT 验签 | 代码审查 auth.ts | ✅ | HMAC-SHA256 + timingSafeEqual 时序安全比较 |
| Token 过期 | 代码审查 | ✅ | exp 校验，过期返回 401 TOKEN_EXPIRED |
| Token 篡改 | 代码审查 | ✅ | 验签失败一律拒绝，不回退 mock |
| 生产环境演示 Token | 代码审查 | ✅ | NODE_ENV=production 拒绝 jt-/jr- 前缀 |
| 越权页面访问 | 代码审查 guards.tsx | ✅ | AuthGuard 未登录跳 /login；RequirePermission 无权限跳 /403 |
| 按钮级权限 | 代码审查 guards.tsx | ✅ | PermissionWrapper（hide/disable）+ usePermission |
| 会话超时 | 代码审查 guards.tsx | ✅ | 剩余 5 分钟提醒，超时自动登出 |
| 限流 | 代码审查 rateLimit.ts | ✅ | 300 次/分钟/IP 滑动窗口，超限 429 |
| CSRF | 代码审查 csrf.ts | ✅ | 双重提交 Cookie；Bearer Token 天然免疫 |
| 安全响应头 | 代码审查 securityHeaders.ts | ✅ | CSP/X-Frame-Options/nosniff/HSTS |

---

## 七、浏览器异常测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 本地存储配额满 | ✅ | storage.ts set 静默失败，不抛异常 |
| 存储读取容错 | ✅ | try/catch 返回默认值 |
| React 渲染异常 | ✅ | ErrorBoundary 捕获，展示降级 UI + 重载按钮 |
| 禁用 JS | ⚠️ P3 | 未配置 <noscript>，记录待补 |
| 禁用 Cookie | ⚠️ P3 | Bearer Token 不依赖 Cookie，天然兼容 |
| 浏览器兼容 | ✅ | Vite 构建 + Autoprefixer，目标现代浏览器 |

---

## 八、问题清单与状态

| 编号 | 级别 | 问题 | 状态 |
|------|------|------|------|
| E-001 | P1 | 前端无离线状态指示器 | ✅ 已修复（useOnlineStatus + OfflineIndicator） |
| E-002 | P2 | 请求超时硬编码 15s | ✅ 已修复（30s 默认 + 环境变量可配） |
| E-003 | P2 | 5xx 重试无退避、仅 1 次 | ✅ 已修复（指数退避，最多 2 次，离线不重试） |
| E-004 | P2 | icons.tsx AntdIconProps 类型导入错误 | ✅ 已修复（内联类型定义） |
| E-005 | P3 | 无 beforeunload 表单未提交提示 | 📝 记录待迭代 |
| E-006 | P3 | 多标签页登录状态实时同步 | 📝 记录待迭代 |
| E-007 | P3 | ErrorBoundary 错误未上报监控 | 📝 记录待迭代（接 Sentry） |
| E-008 | P3 | 无 <noscript> 降级提示 | 📝 记录待迭代 |
| E-009 | P3 | 长列表虚拟滚动/大文件分片 | 📝 按场景逐步落地 |

---

## 九、回归验证

| 验证项 | 命令 | 结果 |
|--------|------|------|
| 前端类型检查 | `web/npx tsc --noEmit` | ✅ 通过（EXIT 0） |
| 新增文件编译 | useOnlineStatus.ts / OfflineIndicator.tsx / request.ts | ✅ 无类型错误 |
| 导出一致性 | hooks/index.ts / components/common/index.ts | ✅ 已导出 |
| AppLayout 集成 | AppLayout.tsx | ✅ OfflineIndicator 已挂载 |

---

*健澜科技数智医院智能体 · 异常与边界测试报告 · 2026-09-16*
