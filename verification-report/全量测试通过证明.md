# 健澜科技数智医院智能体 — 全量测试通过证明

> **证明日期**: 2026-09-16
> **版本**: v0.1.0-final

---

## 一、后端测试证明

### 1.1 类型检查

```
命令: npx tsc --noEmit --ignoreDeprecations 6.0
退出码: 0
错误数: 0
警告数: 0
```

**证明: 后端 TypeScript 类型检查零错误。**

### 1.2 生产构建

```
命令: bun run build
退出码: 0
构建入口:
  - src/entrypoints/cli.tsx → dist/cli.js
  - src/bff/server.ts → dist/server.js
```

**证明: 后端生产构建成功。**

### 1.3 单元测试

```
命令: bun test
退出码: 0

测试结果:
  898 pass
  0 fail
  2377 expect() calls
  Ran 898 tests across 78 files. [2.93s]
```

**证明: 后端 898 个测试用例全部通过，零失败。**

### 1.4 代码覆盖率

```
命令: bun test --coverage
退出码: 0

覆盖率汇总:
  行覆盖率: 86.29%  (目标 ≥85%) ✅
  函数覆盖率: 71.96%
```

**证明: 后端行覆盖率 86.29%，超过 85% 目标。**

---

## 二、前端测试证明

### 2.1 类型检查

```
命令: cd web && npx tsc --noEmit
退出码: 0
错误数: 0
```

**证明: 前端 TypeScript 类型检查零错误。**

### 2.2 生产构建

```
命令: cd web && npm run build
退出码: 0
构建工具: Vite 5.4.21
模块数: 3863
耗时: 14.00s

产物:
  dist/index.html
  dist/assets/index-*.css (37.12 KB)
  dist/assets/index-*.js (主包 260.04 KB / gzip 84.33 KB)
  dist/assets/antd-vendor-*.js (独立chunk)
  dist/assets/echarts-vendor-*.js (独立chunk)
  总产物文件: 83个
```

**证明: 前端生产构建成功，主包 gzip 84.33 KB < 500 KB 目标。**

### 2.3 单元测试

```
命令: cd web && npx vitest run
退出码: 0

测试结果:
  Test Files: 19 passed (19)
  Tests: 157 passed (157)
  Duration: 16.45s
```

**证明: 前端 157 个测试用例全部通过，零失败。**

---

## 三、BFF 运行证明

```
命令: bun run src/bff/server.ts
监听: http://0.0.0.0:8080
状态: 正常启动

验证项:
  GET /health → 200 ✅
  安全响应头 (CSP/X-Frame-Options/X-Content-Type-Options等) → 全部存在 ✅
  POST /api/v1/auth/login → 200 + JWT token ✅
  GET /api/v1/patients (无token) → 401 ✅
  GET /api/v1/patients (有效token) → 200 ✅
  GET /api/v1/patients (篡改token) → 401 ✅
```

**证明: BFF 层可正常启动运行，认证/鉴权/JWT验签/安全头全部正常。**

---

## 四、总测试统计

| 指标 | 数值 |
|------|------|
| 后端测试文件数 | 78 |
| 后端测试用例数 | 898 |
| 后端失败用例数 | 0 |
| 后端行覆盖率 | 86.29% |
| 前端测试文件数 | 19 |
| 前端测试用例数 | 157 |
| 前端失败用例数 | 0 |
| **合计测试用例** | **1055** |
| **合计失败用例** | **0** |

---

**本证明由自动化回归验证流程于 2026-09-16 生成。所有测试均在真实项目环境中执行，退出码为 0 表示全部通过。**
